//

import { Component } from '@angular/core'

export const AppComponent =
Component({
  selector: 'my-app',
  template: `
    <hero-list></hero-list>
    <sales-tax></sales-tax>
  `
})
.Class({ constructor: [function AppComponent () { }] })
